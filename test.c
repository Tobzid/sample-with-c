/* This is just to test run github on vscode */

#include <stdio.h>
#include <string.h>

int main()
{
char new;
char litenew[] = "my name is Tolu";

strcpy(new, litenew);

printf("new now contains %s", new);
return 0;
}